"use client";

import { DesktopEntryBanner } from "./DesktopEntryBanner";

/** Client-only affordances for the mobile shell (discovery, prompts). */
export function MobileShellExtras() {
  return <DesktopEntryBanner />;
}
