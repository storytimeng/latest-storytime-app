# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

storytime.code-workspace

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env*

# Examples and templates for local env files (must NOT match .env* above)
!.env.capgo.example
!.env.android.example

# ── Capgo / Android signing secrets (NEVER commit) ──
# Local-only cheat sheet for the GitHub Actions secrets the workflows need.
.github/workflow-secrets.local.env
# The keystore / passwords that powers the release builds.
.env.android
android/app/upload-keystore.jks
android/app/*.keystore
android/keystore.properties
keystore-base64.txt
tmp-keystore-*

# Supabase CLI local link state (created by `supabase link`)
supabase/

# Build-time artifacts from scripts/android-build.mjs
app/r/client.tsx

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# PWA
/public/sw.js
/public/sw.js.map
/public/workbox-*.js
/public/workbox-*.js.map
/public/worker-*.js
/public/worker-*.js.map
/public/fallback-*.js


yarn.lock
package-lock.json
bun.lockb

#heyapi
openapi-ts-error-*.log