import { DesktopPlaceholderView } from "@/views/desktop";

export default function DesktopSearchPage() {
  return (
    <DesktopPlaceholderView
      title="Search"
      phase="Phase 2 · Discovery"
      description="Desktop search will use a split layout: query and filters on the left, results on the right."
      mobilePath="/search"
    />
  );
}
