import { DesktopPlaceholderView } from "@/views/desktop";

export default function DesktopWritePage() {
  return (
    <DesktopPlaceholderView
      title="Write"
      phase="Phase 4 · Create & edit"
      description="The desktop writing experience will use a step sidebar and a wide editor panel. Continue on mobile via Pen for now."
      mobilePath="/pen"
    />
  );
}
